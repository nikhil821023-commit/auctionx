


# Server
server.port=8080

# Database (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/auctionx
spring.datasource.username=postgres
spring.datasource.password=Nikhil@86
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# (optional) spring.jpa.properties.hibernate.format_sql=true

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB


# JWT
jwt.secret=auctionx_super_secret_key_2024_change_this
jwt.expiration=86400000


# WebSocket
spring.websocket.path=/ws

# Redis (optional - comment out if not using Redis)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Lobby Settings
lobby.min.teams=2
lobby.max.teams=10

# Make upload dir absolute-friendly
file.upload.dir=uploads/

# ? Add this so Spring doesn't block static resource serving
spring.web.resources.add-mappings=true
spring.mvc.static-path-pattern=/uploads/**
